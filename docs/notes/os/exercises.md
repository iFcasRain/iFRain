# 操作系统原理 — 习题库

依据笔记《操作系统.md》全文编制，用于自测与复习。题型包括**单项选择**、**判断**、**填空与简答**、**综合应用**。

---

## 一、操作系统的概念与功能   

### 单项选择

**1.1** 下列关于操作系统的描述中，最准确的是（B ）。

A. 操作系统是一种应用软件  
B. 操作系统负责管理硬件与软件资源，是最基本的系统软件  
C. 操作系统只负责管理内存  
D. 操作系统与用户程序地位相同，无特权  

**1.2** 操作系统作为“系统资源的管理者”，通常不包括下列哪一项管理功能？（ C）

A. 处理机管理  
B. 存储器管理  
C. 编译器优化  
D. 设备管理  

**1.3** “用户说一句，系统做一句”的接口属于（B ）。

A. 脱机命令接口（批处理命令接口）  
B. 联机命令接口（交互式命令接口）  
C. 程序接口  
D. 仅 GUI  

**1.4** 裸机安装操作系统后形成的、功能更强的逻辑机器，在笔记中称为（ B）。

A. 虚拟机（硬件虚拟化意义）  
B. 扩充机器 / 虚拟机（第一章语境）  
C. 微内核  
D. 外核  

### 判断

**1.5** 普通用户可以直接使用“程序接口”而不写代码。（X ）

**1.6** `printf` 等库函数底层可能涉及系统调用。（√ ）

### 简答

**1.7** 简述操作系统的三大定位：资源管理者、对上服务、与硬件的关系。

- 操作系统是系统资源的管理者，负责内存管理、存储管理，处理器管理等
- 向用户提供易于操作的服务         （向上提供GUI 命令接口 程序接口等）
- 管理硬件                                    （最接近硬件的软件 在裸机上扩充机器能力）

---

## 二、操作系统的特征

### 单项选择

**2.1** 单核 CPU 上多个进程“同时运行”，从微观上看属于（ ）。

A. 并行  
B. 并发  
C. 互斥  
D. 同步  

**2.2** 摄像头被 QQ 或微信独占使用，体现了资源共享中的（ ）。

A. 同时共享方式  
B. 互斥共享方式  
C. 虚拟方式  
D. 异步方式  

**2.3** 虚拟存储器主要体现的是（ ）。

A. 时分复用  
B. 空分复用  
C. 既不属空分也不属时分  
D. 仅与磁盘缓存有关  

**2.4** 下列哪一对被笔记强调为“最基本特征且互为存在条件”？（ ）

A. 虚拟与异步  
B. 并发与共享  
C. 并发与虚拟  
D. 共享与异步  

### 判断

**2.5** 多核 CPU 上，不同核心可在同一时刻执行不同任务，这属于并行。（ ）

**2.6** 异步性意味着：相同环境下程序多次运行的结果也可以不同。（ ）

### 填空

**2.7** 时分复用技术的典型例子是逻辑上的________；空分复用技术的典型例子是________。

---

## 三、操作系统的发展与分类

### 单项选择

**3.1** 内存中同一时刻只能有一道作业、I/O 与 CPU 易串行等待的系统属于（ ）。

A. 多道批处理系统  
B. 单道批处理系统  
C. 分时系统  
D. 分布式系统  

**3.2** 强调时间片、交互与响应速度的操作系统类型是（ ）。

A. 多道批处理  
B. 分时操作系统  
C. 网络操作系统  
D. 实时操作系统  

**3.3** 飞行控制、安全联锁等“超时即灾难”的场景，对应（ ）。

A. 软实时  
B. 硬实时  
C. 个人计算机操作系统  
D. 手工操作阶段  

**3.4** 对用户呈现“单一系统映像”、多机紧密协作的是（ ）。

A. 网络操作系统  
B. 分布式操作系统  
C. 单道批处理  
D. 第二类 VMM  

### 简答

**3.5** 多道批处理相对单道批处理的主要收益与主要缺点各是什么？

---

## 四、操作系统的运行机制

### 单项选择

**4.1** 下列哪类指令只能在核心态执行？（ ）

A. 加法指令  
B. 访存指令（用户合法地址）  
C. 关中断或 I/O 等特权指令  
D. 陷入指令  

**4.2** 用户程序从用户态进入内核态以请求 OS 服务，常通过（ ）。

A. 仅能通过外中断  
B. 执行陷入指令（trap）  
C. 直接执行特权指令  
D. 修改 PSW 为内核态  

**4.3** 关于陷入指令，正确的是（ ）。

A. 陷入指令是特权指令，仅内核态可执行  
B. 陷入指令不是特权指令，用户态可执行以进入内核  
C. 陷入指令只能由编译器插入，用户不能触发  
D. 陷入指令与外中断无共同点  

**4.4** 当前是用户态还是内核态，常由（ ）中的相关位反映。

A. 仅 MAR  
B. PSW（程序状态字）等  
C. 仅 PC  
D. 仅通用寄存器 R0  

### 判断

**4.5** 应用程序与内核程序都可直接访问任意物理设备。（ ）

**4.6** 用户态下执行特权指令一般会引发异常，由 OS 处理。（ ）

### 简答

**4.7** 列举至少三种“用户态 → 内核态”的路径。

---

## 五、中断和异常

### 单项选择

**5.1** 缺页后处理完调入页面，返回时重新执行引起缺页的那条指令，这类异常属于（ ）。

A. 陷入（trap）  
B. 故障（fault）  
C. 终止（abort）  
D. 外中断  

**5.2** 时钟中断、键盘中断（狭义）属于（ ）。

A. 内中断  
B. 外中断  
C. 陷入  
D. 故障  

**5.3** 硬件根据中断类型快速找到处理程序入口，主要依赖（ ）。

A. 页表  
B. 中断向量表  
C. PCB 链表  
D. 分区表  

### 判断

**5.4** 系统调用通过陷入引发的内中断，与外中断在软件处理上可能分支到不同逻辑。（ ）

**5.5** 外中断与当前正在执行的指令一定有严格的一对一因果关系。（ ）

### 简答

**5.6** 简述中断机制的四步：检测、响应、向量表、返回（可结合笔记用自己的话）。

---

## 六、系统调用

### 单项选择

**6.1** 系统调用被笔记描述为用户程序访问特权操作的（ ）。

A. 非法后门  
B. 受控的合法编程接口  
C. 仅调试工具  
D. 与库函数完全等价  

**6.2** 下列说法正确的是（ ）。

A. 每次调用 `strcpy` 必然触发一次系统调用  
B. 一次库函数调用可能对应零次或多次系统调用  
C. 库函数永远不能封装系统调用  
D. 系统调用只能在汇编里使用  

### 判断

**6.3** 创建文件、读写设备、创建进程等通常需要系统调用。（ ）

**6.4** 系统调用过程中，陷入指令执行后，用户程序从下一条指令继续的含义是：从陷入指令的下一条用户指令继续。（ ）

### 简答

**6.5** 按笔记顺序写出系统调用过程：传参 → 陷入 → 内核识别与处理 → 返回。

---

## 七、操作系统体系结构

### 单项选择

**7.1** 大内核（宏内核）相对微内核，通常更突出的特点是（ ）。

A. 内核体积极小  
B. 多数服务在内核态、协作路径短、性能往往更好  
C. 文件系统一定在用户态  
D. 不需要中断处理  

**7.2** 微内核中，文件系统、网络等常作为用户态服务器，通过（ ）与内核协作。

A. 仅共享变量无同步  
B. IPC（进程间通信）  
C. 直接修改对方页表  
D. BIOS 调用  

**7.3** 严格分层结构的主要缺点不包括（ ）。

A. 层界难以划分  
B. 调用链可能变长、效率受影响  
C. 只能调用更底层接口  
D. 内核必然无法处理中断  

### 判断

**7.4** 原语用于修改 PCB 等关键结构时，需要保证原子性，可用关中断等方式实现（单核理解下）。（ ）

**7.5** 外核（Exokernel）强调内核尽量小，把更多策略放到用户态库，与 Linux 传统宏内核完全相同。（ ）

### 简答

**7.6** 对比大内核与微内核各两条优点、各两条缺点。

---

## 八、操作系统的引导

### 单项选择

**8.1** 加电后首先执行、完成自检与选取启动设备的通常是（ ）。

A. 用户 shell  
B. ROM 中的 BIOS/UEFI 固件  
C. 进程调度程序  
D. MBR 中的分区表本身  

**8.2** 传统 MBR 中除分区表外，还包含（ ）。

A. 完整 Linux 内核镜像  
B. 磁盘引导程序等  
C. 用户密码哈希  
D. 仅根目录文件列表  

**8.3** 活动分区起始处的引导扇区，笔记中常记为（ ）。

A. GPT 头  
B. PBR（分区引导记录）  
C. PCB  
D. PSW  

### 填空

**8.4** 典型引导链：加电 → 固件 → ________ → 活动分区 ________ → 引导加载器 → ________ → …

---

## 九、虚拟机与 VMM

### 单项选择

**9.1** VirtualBox、VMware Workstation 这类装在已有 OS 上的 VMM，多属（ ）。

A. 第一类（裸机型）  
B. 第二类（宿主型）  
C. 外核  
D. 微内核  

**9.2** 第一类 VMM 通常相对第二类在性能上（ ）。

A. 一定更差  
B. 通常更优  
C. 无区别  
D. 仅用于手机  

### 判断

**9.3** 第一章“扩充机器”语境下的虚拟机与第九章硬件虚拟化语境下的虚拟机，名词含义完全相同。（ ）

---

## 十、进程

### 单项选择

**10.1** 进程存在的唯一标志是（B ）。

A. 程序文件  
B. PCB  
C. JCB  
D. 页表项  

**10.2** PCB 中用于进程切换时保存/恢复的主要是（B ）。

A. 仅进程名  
B. 处理机相关信息（寄存器、PC、PSW 等）  
C. 仅 UID  
D. 仅打开文件路径字符串  

**10.3** 五态模型中，“万事俱备只欠 CPU”的是（B ）。

A. 创建态  
B. 就绪态  
C. 阻塞态  
D. 终止态  

**10.4** 时间片到或被高优先级进程抢占，当前进程应从（ A）。   做错： 应为B  到阻塞态是进程自己选择的

A. 运行 → 阻塞  
B. 运行 → 就绪  
C. 阻塞 → 运行  
D. 就绪 → 终止  

**10.5** 一般教材强调：阻塞态被唤醒后应先进入（B ）。

A. 运行态  
B. 就绪态  
C. 创建态  
D. 终止态  

**10.6** 按等待事件分“等待打印机队列”“等待磁盘 I/O 队列”，属于 PCB 组织的（B ）。

A. 索引方式  
B. 链接方式下对阻塞队列的划分  
C. 仅页表组织  
D. 中断向量表组织  

**10.7** 创建原语的最后一步通常是（B ）。

A. 删除 PCB  
B. 将 PCB 插入就绪队列（条件满足时）  
C. 关机关机  
D. 格式化磁盘  

### 判断

**10.8** 同一程序文件被启动两次，对应两个不同进程。（ √）

**10.9** 阻塞原语与唤醒原语应成对设计，否则可能出现永久阻塞。（ √）

**10.10** 进程切换一定在用户态完成以节省开销。（X ）

### 简答

**10.11** 列出进程五大**特征**。

​	动态性  结构性 异步性 并发性  独立性

**10.12** 写出创建原语的四步（申请 PCB → … → 插入就绪队列）。

创建PCB 申请资源  初始化PCB  插入就绪队列

**10.13** 写出撤销原语的主要步骤（找 PCB、剥夺 CPU、子进程与资源、删 PCB 等，用自己的话亦可）。

查找PCB  剥夺CPU使用  撤销资源和子进程  删除PCB

**10.14** 说明为何进程控制常用原语，以及关中断/开中断在单核模型下的作用。

原语具有原子性，通过开中断关中断使得一旦执行就不会受中断影响而暂停，可以防止进程转换因中断而造成状态异常 

### 综合应用

**10.15** 某单核系统采用时间片轮转。进程 P 正在运行，时间片尚未用完，但 P 因读磁盘执行阻塞原语。问：P 的状态如何变？CPU 接下来最可能做什么？磁盘 I/O 完成后谁调用唤醒原语？P 唤醒后能否立刻占用 CPU？说明理由。

由运行态到阻塞态   转调度执行其他就绪程序   中断处理路径    不能 只是加入了就绪队列

**10.16** 判断下列状态转换是否“通常合法”，并简述原因：  
（1）就绪 → 运行；（2）运行 → 阻塞；（3）阻塞 → 运行；X（4）阻塞 → 就绪；（5）运行 → 就绪。

---

## 参考答案

### 第一章

**1.1** B　**1.2** C　**1.3** B　**1.4** B  
**1.5** ×（程序接口通过代码中的系统调用使用，普通用户不直接“点”程序接口）  
**1.6** √  
**1.7** 要点：管理处理机/存储器/文件/设备等资源；向上提供 GUI、命令接口、程序接口等；是最接近硬件的系统软件，在裸机上扩充机器能力。

### 第二章

**2.1** B　**2.2** B　**2.3** B　**2.4** B  
**2.5** √　**2.6** ×（笔记强调结果应具确定性，变化的是完成时间与停顿位置）  
**2.7** 虚拟处理器（或逻辑上的多 CPU 感受）；虚拟存储器（或逻辑上大地址空间）

### 第三章

**3.1** B　**3.2** B　**3.3** B　**3.4** B  
**3.5** 收益：多道提高 CPU 与资源利用率、吞吐；缺点：无交互或弱交互、周转时间可能长、用户难中途干预。

### 第四章

**4.1** C　**4.2** B　**4.3** B　**4.4** B  
**4.5** ×　**4.6** √  
**4.7** 示例：中断（时钟、I/O）、异常（缺页、非法指令）、陷入（系统调用）。

### 第五章

**5.1** B　**5.2** B　**5.3** B  
**5.4** √　**5.5** ×（外中断与当前指令无必然一一对应，多在指令边界响应）  
**5.6** 指令结束检测 pending → 保存断点与 PSW、关中断等 → 用中断号查向量表跳转服务程序 → 处理完恢复现场并返回或切换进程。

### 第六章

**6.1** B　**6.2** B  
**6.3** √　**6.4** √（返回用户态后从陷入指令的下一条继续）  
**6.5** 传类型号与参数 → trap → 内核识别为系统调用并查表执行服务 → 写返回值、恢复现场、回用户态。

### 第七章

**7.1** B　**7.2** B　**7.3** D（分层并不否定中断；D 为错项）  
**7.4** √　**7.5** ×  
**7.6** 大内核：优—性能好、路径短；缺—庞大难维护、错误影响面大。微内核：优—内核小、可靠易验证；缺—IPC 与模式切换多、开销大。

### 第八章

**8.1** B　**8.2** B　**8.3** B  
**8.4** MBR（或第一级引导代码所在扇区）；PBR；内核（内核映像）

### 第九章

**9.1** B　**9.2** B  
**9.3** ×（第一章为逻辑扩充机器；第九章为运行多个客户 OS 的 VMM 技术）

### 第十章

**10.1** B　**10.2** B　**10.3** B　**10.4** B　**10.5** B　**10.6** B　**10.7** B  
**10.8** √　**10.9** √　**10.10** ×（切换在内核态完成）  
**10.11** 动态性、并发性、独立性、异步性、结构性。  
**10.12** 申请空白 PCB → 分配资源 → 初始化 PCB → 插入就绪队列。  
**10.13** 找 PCB；若运行则剥夺 CPU；处理子进程；回收资源；删 PCB（顺序合理即可）。  
**10.14** 原语保证修改队列/PCB 的原子性；关中断避免中间状态被中断打断导致不一致，末尾开中断恢复响应。  
**10.15** P：运行→阻塞；CPU 转调度执行其他就绪进程；I/O 完成后通常在中断处理路径或驱动中调用唤醒；P 一般先入就绪队列，能否立刻上 CPU 取决于调度，不一定立刻运行。  
**10.16** （1）√；（2）√；（3）一般 ×（应先就绪再被调度运行）；（4）√；（5）√。

---

*题量与难度可按章节裁剪使用；综合题建议闭卷限时作答后再对答案。*
